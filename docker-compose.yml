services:
  app:
    build: .
    container_name: digitalstrides-app
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
